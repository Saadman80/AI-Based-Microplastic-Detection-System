# ── Web app (webapp/) — lean production stack, no torch required ────────────
fastapi>=0.110
uvicorn>=0.29
onnxruntime>=1.18
python-multipart>=0.0.9
pillow>=10.0
numpy>=1.26

# ── Training / PyTorch inference (optional — only for retraining or .pt use) ─
# ultralytics>=8.4.90      # YOLO26 support
# torch>=2.2               # install a build matching your GPU (see docs/KAGGLE_TRAINING_GUIDE.md)
# torchvision>=0.17

# ── Data utilities (optional — dataset building / analysis) ─────────────────
# opencv-python>=4.9
# pyyaml>=6.0
# pandas>=2.0
# matplotlib>=3.8
